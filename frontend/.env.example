# Next.js (BFF) configuration

# Base URL of Slim API as seen from the Next.js server.
# In production on the same host you may point this to an internal URL (127.0.0.1 / unix socket proxy).
PHP_API_BASE_URL=http://localhost:8000

# Must match Slim's INTERNAL_HMAC_SECRET
INTERNAL_HMAC_SECRET=asdfdgf85788689

# Cookie names (optional)
ACCESS_COOKIE_NAME=access_token
REFRESH_COOKIE_NAME=refresh_token
