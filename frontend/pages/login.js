export default function LoginPage() {
  return (
    <main className="container container--full">
      <div className="card card--full">
        <p className="eyebrow">Login</p>
        <h1>Sign in</h1>
        <p>Use your account credentials to log in.</p>
      </div>
    </main>
  );
}
